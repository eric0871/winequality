# Data transformation

As our datasets are well organized in csv files, we just call `read.csv` to load in wine data.
```{r}
library(tidyr)
library(dplyr)
library(reshape2)

df <- read.csv("resource/winequality-red.csv", sep = ';', head = T)
head(df)
```

```{r}
summary(df)
```